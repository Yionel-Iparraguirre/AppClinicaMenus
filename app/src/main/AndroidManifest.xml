<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppClinica"
        tools:targetApi="31">

        <activity
            android:name=".PresentacionActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppClinica">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Otra activity que uses -->
        <activity android:name=".ListaDocsActivity" android:exported="false" />

        <!-- Si tienes otra actividad llamada bienvenido, asegúrate de que no haya conflicto -->
        <!-- <activity android:name=".bienvenido" android:exported="false" /> -->

    </application>

</manifest>